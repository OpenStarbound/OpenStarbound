# LocalAnimator

The `localAnimator` table has had one binding changed.

---

#### `AudioInstance` localAnimator.playAudio(`String` sound, [`int` loops], [`float` volume])

An existing callback, now returns the played audio instance.
